# Ruby Deep Sum Test Instructions

## Your Task
- Implement the `Array#deep_sum` method in `deep_sum.rb`.
- The method should return the sum of **all numeric elements**, including numbers inside nested arrays.

## Rules
- **Do not modify** the test file `spec/deep_sum_spec.rb`.
- Only `deep_sum.rb` should be changed.
- Empty arrays should return `0`.

## Run tests:

```bash
rspec
```

## Notes

- Write clean and readable Ruby code.
